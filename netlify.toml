[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Keep SPA routing working on Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
